 write a function to get input of long string and convert it to paras. para will be group of sentence count, count is passed as input 
 * run command --> StringToPara **"filename" "sentence per para"**
